// Run it: dart run temp_dart.dart

void main()
{
  print("""
You are funny.
Whole point of the `D` alternative is to get rid of all Pointer and calloc/free nonsense.
No need to care for anything. Just create your `D` alternative objects and use them in `D` modules.
That's all.
  """);
}